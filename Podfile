# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'RandomColorRegions' do
  use_frameworks!

  pod 'Reveal-SDK', '~> 13', :configurations => ['Debug']
end
